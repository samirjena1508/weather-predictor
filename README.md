# weather-predictor
A weather prediction desktop application built using Python and a graphical user interface (GUI). The application fetches weather data, predicts conditions, and displays temperature, humidity, and forecast information through an easy-to-use interface.
